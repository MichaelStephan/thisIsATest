<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 
        Security Configuration for CVE-2023-6481 Remediation
        
        This configuration explicitly ensures logback-receiver is NOT enabled.
        CVE-2023-6481 affects logback-core versions < 1.2.13, 1.3.14, or 1.4.14
        Current version: 1.5.18 (via Spring Boot 3.5.6) - NOT VULNERABLE
        
        Logback-receiver is NOT configured in this application.
        If remote logging capabilities are needed in the future, ensure:
        1. Access is properly restricted
        2. Authentication is enforced
        3. Rate limiting is implemented
    -->
    
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
    
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
    
    <logger name="com.acme.payrollmngmt" level="INFO"/>
</configuration>
